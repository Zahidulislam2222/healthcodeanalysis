# Docker local development passwords
# Copy to .env.docker and customize if desired.
# If not set, docker-compose.yml uses these as defaults automatically.
MYSQL_ROOT_PASSWORD=rootpass
MYSQL_DATABASE=wordpress
MYSQL_USER=wordpress
MYSQL_PASSWORD=wordpress
